{
  "passwords": [
    {
      "name": "GD_Matemblewo_rpt",
      "prefix": "53640000",
      "level": "admin",
      "password": "CHANGE_ME"
    }
  ],
  "default_guest_passwords": ["", "hello", "password"],
  "_comments": {
    "usage": "python meshcore_discovery.py --passwords passwords.json --companion 53640000 ...",
    "strategy": "For each repeater: try passwords in order, then trace fallback",
    "default_guest_passwords": "Tried on every repeater without a specific entry, blank first (most common)",
    "levels": "admin = full CLI access, guest = stats + neighbors read access",
    "add_known": "Add specific entries for repeaters where you know the password"
  }
}
